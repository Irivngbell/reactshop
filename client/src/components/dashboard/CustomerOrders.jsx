import React from 'react';
import PropTypes from 'prop-types';

const CustomerOrders = props => {
    return <section>Les commandes</section>;
};

CustomerOrders.propTypes = {};

export default CustomerOrders;
